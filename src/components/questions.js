import React from 'react';

function Questions({ info }) {

    console.log(info);
    return <>
        {/* <div className='q_cont mb-3'>
            {props.info.map((index, quest) => {
                <div>{quest[index]['question']}</div>
            })}
        </div> */}
    </>;
}

export default Questions;
